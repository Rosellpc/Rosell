# Prototipos originales

Archivos conservados antes de la migración. No forman parte del build ni del sitio publicado.

- `index.html`, `style.css`, `script.js`: landing original.
- `main.html`, `main.css`, `main.js`: exploración original del vórtice.
- `img/`: fotografía original.

El prototipo `main.html` conserva su referencia original a `style.css`. La implementación activa y corregida vive en `src/`.
